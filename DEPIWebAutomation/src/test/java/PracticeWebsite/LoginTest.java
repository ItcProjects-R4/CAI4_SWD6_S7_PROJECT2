package PracticeWebsite;import org.testng.annotations.AfterMethod;import org.testng.annotations.BeforeMethod;import org.testng.annotations.DataProvider;import org.testng.annotations.Test;import org.testng.Assert;import org.openqa.selenium.WebDriver;import org.openqa.selenium.chrome.ChromeDriver;public class LoginTest {    private WebDriver driver;    private LoginManager loginManager;    @BeforeMethod    public void setUp() {        // Initialize the browser driver (Selenium 4 handles driver binaries automatically)        driver = new ChromeDriver();        driver.manage().window().maximize();        // Navigate to the base URL        driver.get("https://practice.expandtesting.com/login");        // Initialize the Page Object Manager        loginManager = new LoginManager(driver);    }    /**     * DataProvider containing the hardcoded test cases.     * Columns: Test Name | Username | Password | Expected to Succeed?     */    @DataProvider(name = "loginScenarios")    public Object[][] getLoginData() {        return new Object[][] {                // 1. Valid Login                {"Valid Login", "practice", "SuperSecretPassword!", true},                // 2. Invalid Username                {"Invalid Username", "wrongUser", "SuperSecretPassword!", false},                // 3. Invalid Password                {"Invalid Password", "practice", "WrongPassword123", false},                // 4. Empty Fields                {"Empty Fields", "", "", false}        };    }    @Test(dataProvider = "loginScenarios")    public void testLoginFlows(String testName, String username, String password, boolean expectedSuccess) {        System.out.println("Executing Test: " + testName);        // 1. Ensure we are on the login page        loginManager.openLoginPage();        // 2. Perform the login action with the provided credentials        loginManager.login(username, password);        // 3. Evaluate the result based on our expected outcome        if (expectedSuccess) {            // Assert that the successful login indicators are present            Assert.assertTrue(loginManager.isLoginSuccessful(),                    "[" + testName + "] Expected login to succeed for user '" + username + "', but it failed.");            // Clean up by logging out so the state is clear (optional but recommended)            loginManager.logout();        } else {            // Assert that the error message is displayed for invalid attempts            Assert.assertTrue(loginManager.isLoginErrorDisplayed(),                    "[" + testName + "] Expected an error message to be displayed, but none was found.");        }    }    @AfterMethod    public void tearDown() {        // Close the browser after each test iteration        if (driver != null) {            driver.quit();        }    }}